/*function createGroceryList() {
  let groceryList = [];
  let createList = prompt("Do you want to create a grocery list? (y/n):");
  if (createList.toLowerCase() === 'y') {
    let numberOfItems = prompt("How many items do you want to add to your grocery list?");
    numberOfItems = Number(numberOfItems);
    for (let i = 0, i < numberOfItems; i++) {
      let item = prompt("Enter grocery item" + (i + 1) + ":");
      groceryList.push(item);
    }
    
    let groceryListSorted = [...groceryList].sort();
    let groceryListReversed = [...groceryList].reverse();
    
    alert("Grocery List:" + groceryList.join(","));
    alert("Sorted Grocery List:" + groceryListSorted.join(","));
    alert("Reversed Grocery List:" + groceryListReversed.join(","));
  } else {
    console.log("No grocery list created.");
  }
}

createGroceryList();
*/
