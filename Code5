let favoriteColors = [];

for (let i = 0; i < 3; i++) {
    let colors = prompt("Enter your three favorite colors:");
    favoriteColors.push(colors); 
    console.log("Updated color list: ", favoriteColors);
}

console.log("Your favorite colors are: ", favoriteColors);
